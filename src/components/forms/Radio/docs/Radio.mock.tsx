/* eslint-disable no-alert */
import { IRadio } from "../types";

const base: IRadio = {
  text: "radio",
  disabled: false,
  name: "radio",
  id: "radioid",
  value: "valor",
};

export const mockRadioProps = {
  base,
};
